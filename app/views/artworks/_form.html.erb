<%= form_for @artwork do |f| %>

  <% if @artwork.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@artwork.errors.count, "errore") %> non è possibile salvare l'opera:
      </h2>
      <ul>
        <% @artwork.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label 'Nome' %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label 'Dimensione' %>
    <%= f.text_area :dimension %>
  </p>
  <p>
    <%= f.label 'Descrizione' %>
    <%= f.text_area :description %>
  </p>
    <p>
    <%= f.label 'Note' %>
    <%= f.text_area :notes %>
  </p>
  <p>
    <%= f.check_box :production %>
    <%= label_tag(:production, "Opera produzione?") %>
  </p>
  <p>
    <%= f.label 'Quantità' %>
    <%= f.text_area :quantity %>
  </p>
  <p>
    <%= f.label 'Tipo' %>
    <%= f.select(:typology, options_for_select(@available_typologies), include_blank: true) %>
  </p>
  <p>
    <%= f.label 'Tecnica' %>
    <%= f.select(:technique, options_for_select(@available_techniques), include_blank: true) %>
  </p>
  <p>
    <%= f.label 'Link IUAV' %>
    <%= f.text_area :linkIUAV %>
  </p>
  <p>
    <%= f.label 'Da ' %>
    <%= f.date_select(:infdate, start_year: @limit_firstyear.year, end_year: @limit_lastyear.year , discard_day: true,discard_month: true,) %>
    <%= f.label 'a ' %>
    <%= f.date_select(:supdate, start_year: @limit_firstyear.year, end_year: @limit_lastyear.year , discard_day: true,discard_month: true,) %>
  </p>
  <p>
    <%= f.check_box :dateexact %>
    <%= label_tag(:dateexact, "Data esatta?") %>
  </p>

  <p>
    <%= f.submit class: "btn btn-success btn-sm"%>
  </p>

<% end %>